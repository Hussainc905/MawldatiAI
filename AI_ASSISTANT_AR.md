# مساعد المولدة الذكي — v0.5.0

## الوضع المحلي
يعمل مباشرة بدون إنترنت ولا يحتاج أي إعداد. يغطي معلومات عامة عن الحرارة، الدخان، الزيت، الوقود، البطارية، AVR، الفولتية، التردد، RPM، الأحمال والصيانة.

## الوضع عبر الإنترنت (اختياري)
من داخل التطبيق: الإعدادات → المساعد الذكي → إعداد مساعد الإنترنت.
أدخل رابط API أو Supabase Edge Function يستقبل POST بصيغة JSON:

```json
{
  "question": "سؤال المستخدم",
  "domain": "diesel_generator_maintenance"
}
```

ويُفضّل أن يعيد:

```json
{
  "answer": "الإجابة"
}
```

لا يرسل التطبيق أسماء المشتركين أو الدفعات أو الوصولات إلى هذا الرابط؛ يرسل السؤال المكتوب فقط.
